{
    "name": "imamx39/php-framework",
    "description": "A custom PHP framework inspired by Symfony — ORM, Router, Auth, Events, Cache, Forms, Mailer and more.",
    "type": "project",
    "license": "MIT",
    "keywords": ["framework", "php", "orm", "router", "mvc"],
    "authors": [
        {
            "name": "IMAMx39",
            "role": "Developer"
        }
    ],
    "require": {
        "php": ">=8.1",
        "twig/twig": "^3.24"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "psr-4": {
            "Framework\\": "src/",
            "App\\": "app/",
            "Migrations\\": "migrations/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "serve": "php -S localhost:8000 -t public/",
        "post-create-project-cmd": [
            "@php bin/setup"
        ]
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}
